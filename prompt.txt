This may apply to the following skills: /breakdown-feature-request, /breakdown-feature, /execute-task, /review-task-pr

There seems to be a lot of re-reading of the code base when I run execute task. This would likely be made more efficient by including in the feature
tags to specific files to make sure that there isn't overreading of the repository.

When I run execute-task, it creates a custom bash script for setting up the worktree and stuff it looks like. This should be prepared and referenced 
instead of made from scratch.

There are many gh commands that are called as part of all of these skills. This feels like it may be loading a fair amount of garbage into the context.
Can each skill have a prepared script that can be run to load the full context of what it needs ahead of time instead of calling a bunch of gh commands?

On review-task-pr, the agent keeps trying to run the tests before ensuring that the branch is correct and up-to-date
